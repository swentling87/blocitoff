%h4
  #{item.name.capitalize}
  %small.pull-right #{distance_of_time_in_words(Time.now, item.created_at + 7.days)}
  =link_to "", user_item_path(current_user.id, item.id), method: :delete, class: 'glyphicon glyphicon-ok', remote: true
