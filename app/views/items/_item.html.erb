<h4 id="item-<%= item.id%>"><%= "#{item.name.capitalize}" %>
  <small class="pull-right"><%= "#{distance_of_time_in_words(Time.now, item.created_at + 7.days)}" %></small>
  <% if @user == current_user %>
    <%= link_to "", user_item_path(@user.id, item.id), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
  <% end %>
</h4>
